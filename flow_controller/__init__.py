"""Flow controller application package."""

#: Shown in the window chrome.  Kept here rather than in a view module so the
#: Tk app and the Qt app cannot drift into claiming different versions.
APP_VERSION = "3.0.0"
